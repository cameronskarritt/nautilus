pre-push:
  parallel: true
  commands:
    go-fmt:
      glob: "*.go"
      run: goimports -d {staged_files}

    go-lint:
      glob: "*.go"
      run: golangci-lint run {staged_files}

    # go-test:
    #   glob: "*.go"
    #   run: go test ./...
